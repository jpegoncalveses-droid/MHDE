weights:
  cheap: 0.30
  quality: 0.25
  catalyst: 0.25
  momentum: 0.10
  sentiment: 0.10
  risk_penalty: 0.20

tiers:
  A:
    min_total: 75
    min_catalyst: 50
    max_risk: 50
  B:
    min_total: 60
  C:
    min_total: 45

# Tier A conditions: total >= 75 AND catalyst >= 50 AND risk_penalty <= 50
# Tier B: total >= 60
# Tier C: total >= 45
# Reject: total < 45 OR risk_penalty > 75 OR insufficient_data
