# STATE.md: Current System State

> Overwrite this file IN PLACE only when the operator says
> "refresh STATE". This is NOT append-only.
> It answers one question: what is true right now?
>
> Precedence on any conflict: the live host wins, then SESSION_LOG.md,
> then this file. A conflict between docs is a bug to fix.
>
> Machine fields come from scripts/snapshot_state.py. Judgement fields
> (blockers, next action, divergences) are filled by CC from the
> session, or by asking the operator.

Last updated: 2026-05-27.

## Orientation
- Phase (per docs/PATH_TO_LIVE_PLAN.md): Phase 0 (calibration validation,
  parallel) — Active; Phase 1B (execution backtest) — base grid complete,
  sensitivity grid pending. Phase 2 (execution-layer build) not started
  (gated on Phase 0 passing). Phases 3 (paper) and 4 (live) not started.
- Live branch: `master`.
- Working tree clean / committed / pushed: `master` is the deployed line.
  At snapshot time the host's primary checkout carried uncommitted
  in-flight work on `fix/dashboard-pnl-cumulative-delta` (dashboard PnL).
  This STATE.md tooling was built on an isolated branch off `master`.

## In-flight work
- Active branches and status (unmerged, by recent activity):
  - `exp/trailstop-sweep` — backtest trail-stop sweep experiment.
  - `exp/parabolic-filter-ab` — parabolic-filter A/B (WIP stashed).
  - `fix/dashboard-pnl-cumulative-delta` — dashboard PnL cumulative-delta
    fix; uncommitted changes parked.
  - `feat-btc-regime-classifier` — BTC regime classifier (writes
    `crypto_regime_daily`, currently an orphan table).
  - `feat-universe-correction`, `feat-universe-hysteresis-continuous`,
    `chore/session-log-universe-state` — universe-builder workstream.
  - `gap3-paper-trading-dashboard-tab`, `gap2-paper-trading-drift-monitor`
    — paper-trading observability (Gap 2/3).
  - `chore/spec-remove-phantom-polling-mhde` — spec cleanup (local +
    pushed); see SESSION_LOG 2026-05-19.
  - `chore/state-md-snapshot-tooling` — this STATE.md tooling.
- Stashes: 2 — `stash@{0}` park-dashboard-pnl-for-statemd-chat (on
  fix/dashboard-pnl-cumulative-delta), `stash@{1}` parabolic-filter WIP
  (on exp/parabolic-filter-ab).
- Built but not deployed: none known.

## Active config
- Universe: `crypto_universe` has 60 rows, 51 active. Last added_date
  2026-05-19; last removed_date 2026-05-21. Source
  `binance_usdtm_perp_top_50`.
- Strategy spec: `data/exports/active_spec.json` (v1.0.0, hash
  sha256:60a2c813…, generated by MHDE commit d8ba573). phase_1b_winner:
  exit_policy D, horizon 10d, top_n selection_n=6, activation_pct 0.01,
  trail_pct 0.3. Sizing: deploy 0.8 / reserve 0.2, leverage 1.0, isolated,
  max_concurrent 6 / min 5. Risk: daily_loss_limit $100, max_account_dd
  30%, position_size_max 20% / min $5. Runtime: entry 06:30 UTC,
  reconciliation 23:00 UTC, 24h monitoring window.
- Active model run id: two active crypto runs — `crypto_10d_e1edf4e6`,
  `crypto_5d_306d3f1a`.
- Paper-trading status: not live (Phase 3 gated on Phase 2 build).
  Drift-monitor observability scaffolding present and scheduled
  (`mhde-monitor-paper-trading-drift.timer` active); P&L-band / drawdown /
  monthly arms deferred per KI-136 ("Gap 2.5").

## Deltas from baseline
- DB tables not in the documented schema (live tables absent from
  crypto/ml/fx `schema.py`): 67 live vs 30 declared; 37 orphans. Equity-side
  + legacy + experimental tables: alerts, backtest_runs, candidate_outcomes,
  candidate_reviews, companies, crypto_backtest_runs, crypto_backtest_summary,
  crypto_backtest_trades, crypto_regime_daily, dashboard_actions,
  earnings_estimates, events, features, filings, fundamentals_features,
  fundamentals_raw, fx_prices_hourly_twelvedata_backfill, health_checks,
  hypotheses, llm_runs, macro_series, missed_opportunity_events,
  missed_opportunity_investigations, missed_opportunity_root_causes,
  model_runs, monitor_alert_state, move_episodes, pipeline_runs, prices_daily,
  promotion_gate_results, rejections, review_notes, schema_version,
  scorecard_experiments, scores, short_interest, source_runs. No declared
  table is absent from the DB. (Run scripts/snapshot_state.py for the live
  diff — the 3 schema modules intentionally cover only crypto/ml/fx.)
- Timers differing from the documented set: 25 mhde-* timers active (23
  system, 2 user — daily-analysis, health-check). Not diffed against the
  INFRASTRUCTURE.md documented set this session; none flagged.

## Blockers and next action
- Open blockers (reference KI-IDs): live-path gates are Phase 0 calibration
  passing and the Phase 1B sensitivity grid. 19 open KI observations,
  including: KI-122 (universe reconciliation leaks stale extended-tier
  rows), KI-126 (Phase 0 drift definition (b) not implemented), KI-131
  (crypto 5d under-emitted predictions), KI-132 (Streamlit not auto-restarted
  after dashboard merges), KI-136 (paper-trading drift arms deferred),
  KI-137 (post-parabolic-crash buy re-emission; mitigated not fixed at
  source), KI-141 (crypto_ml_predictions lacks a run-time stamp).
- Next agreed action: operator merges `chore/state-md-snapshot-tooling`,
  then resumes the parked `fix/dashboard-pnl-cumulative-delta` work.
- Deferred queue (max 3): (1) KI-136 — paper-trading drift P&L-band /
  drawdown / monthly arms; (2) KI-141 — add run-time stamp to
  crypto_ml_predictions; (3) KI-137 — direction-aware / risk-adjusted
  parabolic filter at the model-label source.

## Repo / host divergences
- None confirmed this session. Machine fields above are read directly from
  the live host (DuckDB read-only + systemctl), so they reflect host truth
  at snapshot time. Repo-vs-host drift has its own monitor
  (`mhde-monitor-config-drift.timer`); no divergence was surfaced here.
